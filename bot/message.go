package bot

func (bot *smartBot) handleMessage() {
	//	if update.Message != nil && update.Message.Chat != nil { // 未定义消息的处理
	//		// if chat is nil, panic
	//		bot.SendText(update.Message.Chat.ID, "这个问题，暂时无法处理")
	//	}
}
